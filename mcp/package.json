{
  "name": "@deploy-panel/mcp",
  "version": "0.1.0",
  "description": "MCP server for Deploy Panel — deploy, rollback, preflight via AI agents",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "deploy-panel-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.0.0"
  }
}
