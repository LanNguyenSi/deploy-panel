export default function HomePage() {
  return (
    <main className="page-shell">
      <h1 style={{ fontSize: "var(--text-lg)", fontWeight: 700, marginBottom: "var(--space-4)" }}>
        Deploy Panel
      </h1>
      <p style={{ color: "var(--muted)" }}>
        VPS deployment management — coming soon.
      </p>
    </main>
  );
}
